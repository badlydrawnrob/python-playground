meta {
  name: to-do-list-delete-id
  type: http
  seq: 6
}

delete {
  url: http://localhost:8000/todo/:id
  body: none
  auth: none
}

params:path {
  id: 1
}

docs {
  # To Do
  
  > There should be no duplicate `:id`s, but if there were, this will delete the first match it finds.
  
  **Delete** the `ToDo` with this `:id`
  
  
  
}
