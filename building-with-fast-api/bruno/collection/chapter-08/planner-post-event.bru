meta {
  name: planner-post-event
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/event/new/
  body: json
  auth: inherit
}

body:json {
  {
    "creator": null,
    "title": "Glastonbury",
    "image": "https://tinyurl.com/ed-sheeran-with-shakira",
    "description": "Ed Sheeran sings with Shakira at Glastonbury!",
    "location": "Live",
    "tags": ["music", "adults", "event"]
  }
}

docs {
  # 🐞 Bugs
  
  1. **`NULL`able by default**
      - Currently `Event.creator` foreign key can be `None`
      - However, the endpoint _always_ adds the `BaseUser.id` of the logged in user.
  2. 🕰 **Slow response time?**
      - Because we have to get the `BaseUser.id` first!
}
