meta {
  name: to-do-list-get
  type: http
  seq: 2
}

get {
  url: http://localhost:8000/todo
  body: none
  auth: none
}

docs {
  # To Do
  
  > Returns the `Item`
  
  Here we ignore the wrapping `ToDo` and it's `:id`, instead returning a simple list of items.
}
