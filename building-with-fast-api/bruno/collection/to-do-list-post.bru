meta {
  name: to-do-list-post
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/todo
  body: json
  auth: none
}

body:json {
  {
    "id": 1,
    "item": {
      "item": "My first to-do is: finish the book",
      "status": "Chapter 02"
    }
  }
}

docs {
  # To Do
  
  > Keys are case-sensitive
  
  1. Must contain an `int` with key `id`
  2. Must contain an `Item` with key `item`
      - `Item` is `item: str`, `status: str`
  
  Currently accepts empty strings and duplicate `id`s 😔
}
